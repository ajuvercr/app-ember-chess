{{page-title 'Rock & Roll with Octane'}}
<div class="bg-blue-900 text-gray-100 p-8 min-h-screen">
  <div class="mx-auto mb-4">
    <div class="h-12 flex gap-4 items-center mb-4 border-b-2">
      <LinkTo @route="chesshub">
        <h1 class="text-2xl">
          Home
        </h1>
      </LinkTo>

      <LinkTo @route="account">
        <h1 class="text-2xl">
          Account
        </h1>
      </LinkTo>

      <div style="margin-left: auto;">
        <h1 class="text-2xl">
          {{this.auth.user.name}}
        </h1>
      </div>
    </div>
    <div class="flex flex-wrap">
      {{outlet}}
    </div>
  </div>
</div>
